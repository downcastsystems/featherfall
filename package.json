{
  "name": "one-big-sky",
  "version": "0.2.0",
  "private": true,
  "description": "Local multiplayer flying-mount arcade game for macOS and Windows",
  "scripts": {
    "start": "node server.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check audio.js && node --check engine.js && node --check match-series.js && node --check broadcast.js && node --check game.js && node --check server.cjs && node scripts/check-ui.cjs"
  }
}
