{
  "name": "featherfall",
  "version": "0.1.0",
  "private": true,
  "description": "Local multiplayer bird-riding arcade game for macOS and Windows",
  "scripts": {
    "start": "node server.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check audio.js && node --check engine.js && node --check game.js && node --check server.cjs"
  }
}
